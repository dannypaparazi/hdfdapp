# Copy to .env before running any tool: cp .env.example .env
# No keys are required yet -- add them here as new tools need credentials.
# Never commit the real .env; tools/_common.py's load_env() reads it via
# python-dotenv from the project root.
